

# вычисления

def sum(a, b):
    return f"{a} + {b} = {a + b}"
    
def sub(a, b):
    return f"{a} - {b} = {a - b}"

def mult(a, b):
    return f"{a} * {b} = {a * b}"

def div_rem(a, b):   
        return f"{a} % {b} = {a % b}"
    
def div_all(a, b):                     #обычное деление
    return f"{a} / {b} = {a / b}"
    
def div_int(a, b):                     #целочисленное деление
    return f"{a} / {b} = {a / b}"

def pow(a, b):
    return f"{a}^{b} = {a ** b}"